- content_for(:title, t('.title'))

.div.min-h-screen.bg-gradient-to-b.from-blue-50.to-white.py-8.px-4
  .max-w-6xl.mx-auto

    / 検索フォーム
    .bg-white.shadow-md.rounded-lg.p-6.mb-8
      form
        .flex.flex-col.sm:flex-row.items-center.gap-4
          input[
            type="search"
            placeholder="キーワード検索"
            class="w-full flex-grow border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400 transition"
          ]
          input[
            type="search"
            placeholder="カテゴリ" # 犬種、フード、飼い方etc
            class="w-full flex-grow border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400 transition"
          ]
          input[
            type="search"
            placeholder="サブカテゴリ" # 犬種→チワワ、フード→危険な食べ物etc
            class="w-full flex-grow border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400 transition"
          ]
          input[
            type="submit"
            value="検索"
            class="w-full sm:w-auto px-6 py-2 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition cursor-pointer"
          ]
          input[
            type="submit"
            value="リセット"
            link_to board_path
            class="w-full sm:w-auto px-6 py-2 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition cursor-pointer"
          ]

    / 掲示板一覧
    .grid.grid-cols-1.sm:grid-cols-2.lg:grid-cols-3.gap-4
      - @boards.each do |board|
        = render board

    / ページネーション（必要に応じて）
    .mt-6.flex.justify-center
      nav.bg-white.shadow-md.rounded-md.px-4.py-2.flex.space-x-4
        = paginate @boards
